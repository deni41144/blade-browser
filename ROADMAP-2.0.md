# ROADMAP 2.0 — «Переплавка»

> План суперглобального релиза Blade 2.0: полный рефакторинг, выжимание скорости,
> высшее качество интерфейса, собственный сетап. Создан 2026-09-13 по слову владельца.
> Статус: **на утверждении**. Публикация только 2.0.0 в конце; промежуточные волны —
> сборки + визуальный проход владельца.

## 0. Цели и метрики успеха

| Метрика | Сейчас (замер 2026-09-12) | Цель 2.0 |
|---|---|---|
| Paint окна старта | 389 ms | **≤ 300 ms** |
| SSR (первый стабильный кадр) | 411 ms | **≤ 330 ms** |
| DCL / Load | 194 / 267 ms | ≤ 170 / ≤ 230 ms |
| Строки JS (11 скриптов) | 4543 | −15% без потери фич (дедуп) |
| Замер декомпозиции (утро 13.09, внешний таймер Patchesoot-timing.ps1) | полный Blade: окно за **251 мс**; свежий пустой профиль движка: **460 мс** | наш стек не тормозит старт — быстрее голого движка |
| Дефолты старт-префов FF155 (сверено с omni) | preXulSkeletonUI=true, restore_on_demand=true, restore_tabs_lazily=true, newtab.preload=true — ВСЕ уже оптимальны | **нечего.flip — движок кораблётся настроенным** |
| Строки CSS (без covers) | 3614 | −15% (мёртвые правила волн 1.7–1.9) |
| Шрифты | 1.68 MB (Unbounded 778 KB!) | ≤ 0.7 MB (сабсет cyr+lat) |
| Известные визуальные дыры | 4 (AI Overview, reader, PDF, print) | **0** |
| Дистрибуция | только патчи | **Patch + Full + Setup.exe в одном релизе** |
| Друзья: движок | старый, ждут Full (ограничение №5) | свежий FF155 + v3-хирургия |

Замер — `BladePerf` → `perf_mark.txt`. Каждая волна 2.0 пишет до/после в
`perf_history.txt` (новый формат, фаза 0). Метрика = медиана 5 запусков.

## 1. Аудит текущего состояния (факты, не ощущения)

- **JS 4543 строк / 11 скриптов.** Монолит: `BobliksSettings` 1599 строк = 35% всего
  кода (меню B, темы, фоны, сплеш, ghost, DNS, бэкап — всё в одном).
- **CSS 3614 строк руками + 549 генерится** (covers). Волны 1.7–1.9 писали блоки
  поверх блоков — дубли селекторов и мёртвые правила почти гарантированы.
- **Шрифты 1.68 MB.** Unbounded 778 KB — вариативный, полный; реально нужны
  cyrillic + latin + базовая пунктуация.
- **Слушатели:** window-scoped умирают с окном (утечек нет), но интервалы/страховки
  размазаны по скриптам без реестра; mountClock до сих пор тикает страховками
  setTimeout 1500/5000 из часового дела.
- **Старт:** сплеш и темы уже этапные; paint 389 ms — в бюджет, но CSS-диета и
  отложенные операции дадут ещё.
- **Сетап:** Blade-Setup.exe (.NET 9, single-file, 65 MB) жив и читает VERSION из
  data.zip — но его data.zip от 11 сентября (эпоха ~1.4.x, на восемь волн старше).
- **Конвейер Full** (`Publish -Mode Full`) уже даёт data.zip побайтово идентичный
  патчу — фундамент для 2.0-сетапа готов, надо собрать свежий и прогнать.
- **Движок:** FF155 с v3-хирургией у владельца; друзья на старом (ограничение №5).

## 2. Фазы (порядок зафиксирован: сначала инструменты, потом мясо)

### Фаза 0 — «Стенд»: бенчмарки и Пульс (инструменты, 1 волна)
Без замеров оптимизация — гадание. Сначала измерительный контур.

- [x] **BladePerf 2.0:** история замеров `perf_history.txt` (ротация 50),
      фазы dcl/load/paint/ssr. Стартовый прогон = базовая линия «до».
- [x] **Пульс Клинка** (`BladePulse.uc.js`): контракт `window.Blade`, узлы
      (#blade-hero-wrap, часы, меню B, навбар), шрифты, VERSION/covers →
      `blade_health.txt` + вердикт GREEN/YELLOW/RED. Первый живой прогон:
      GREEN, все 10 проверок ОК (2026-09-13 03:53).
- [x] **FastHelper-аудит базовой линии:** см. фазу 2 (находки уже там).
- [x] **Бейзлайн 2.0 (медиана 4 прогонов, 2026-09-13 03:54):**
      dcl=215ms load=285ms paint=410ms ssr=433ms.
- [x] Уроки ночи → конвенция 13 (PS 5.1 quoting, лаунчер-PID, dev-инстанс).

### Фаза 1 — «Ядро»: JS-рефакторинг (2–3 волны)
Цель: тот же функционал, меньше кода, ноль тиков вхолостую.

- [x] **1-А (гит 5eb0e64):** реестр `Blade.every/Blade.listen` с авто-снятием на
      unload; мигрированы интервалы Clock (погода) / Newtab (тик) / Settings
      (idle, авто-тема, idle-часы); срезан слепой 5с-ретрай mountClock.
      Верификация: Пульс GREEN, перф без регрессии (dcl 213/221 против 215).
- [ ] **1-Б: декомпозиция BobliksSettings** — карта Analyst готова (10 точек
      риска расписаны, порядок секций: Core→Themes→Bgs→System→Splash→Ghost→
      Menu→init→API→mark). САМЫЙ рискованный шаг — вживлять по одной секции,
      после каждой Пульс + живой клик-тест. Если ночь не потянет — отложить
      с обоснованием в отчёт (UI-ценность волны нулевая, риск высокий).
- [ ] Отложенный старт остальных тяжёлых операций (аудит после 1-Б).

### Фаза 2 — «Стиль»: CSS-диета (1–2 волны)
- [x] **Аудит мёртвого CSS (FastHelper, верифицировано GLM):** за 9 волн накопилось
      всего 2 мёртвых вещи — класс `.blade-newwin` (userChrome.css:109,126-128 —
      дубль #nav-bar::before, никто не вешает) и переменная `--bob-accent-soft`
      (пишется в userContent ~10 строк + Settings:213,269,279, не читается нигде).
      ВНИМАНИЕ: `--accent-soft` (хромовый) — ЖИВОЙ, это другая переменная.
      53/53 keyframes живые. CSS чище ожидаемого — фаза смещается к шрифтам/склейке.
- [x] **Сабсет шрифтов (гит 564f818):** 1.68 МБ → 551 КБ (−67%). Unbounded 778→209,
      Rubik 360→126, Rubik-Italic 355→124, JBM 187→106. Юникод: latin+cyrillic+
      пунктуация+₴/№/❯; вариативные оси wght целы, name-таблицы целы (Пульс
      GREEN — все три семьи регистрируются). Оригиналы: Backups\fonts-full-2026-09-13.
      JBM: ₴ в оригинале отсутствовал (системный фолбэк) — не потеря.
- [x] **Мёртвые артефакты срезаны:** блок .blade-newwin (дубль правила) +
      18 деклараций --bob-accent-soft + 3 сайта записи в Settings.
- [ ] **Склейка дублей** волн 1.7–1.9 — остаётся (крон-смена, по приоритету ниже 1-Б).
- [ ] **Мёртвые правила:** удалить классы без потребителей (список из фазы 0,
      ручная верификация каждого — правки только после grep-подтверждения).
- [ ] **Склейка дубликатов** волн 1.7–1.9: 13 микро-вау несли свои блоки поверх
      старых — объединить селекторы, свернуть повторы.
- [ ] **userContent:** сгруппировать по @-moz-document, убрать копипасту доменных
      блоков (Google-слои, погода-оверлеи).
- [ ] **Дорогие селекторы:** universal/глубокые descendant в горячих путях (табы,
      урлбар) → заменить классами-хуками, где движок позволяет.
- [ ] **Сабсет шрифтов:** Unbounded/Rubik/Rubik-Italic/JetBrains → cyr+lat+пунктуация.
      Проверить спецсимволы UI (✨ ☀ ✦ берутся системным fallback — убедиться, что
      сабсет их не сломал). Цель ≤ 0.7 MB суммарно.
- [ ] covers.css: минификация вывода генератора.
- [ ] Прогон: Gemini-скринрейд ключевых поверхностей + бенчмарк. Коммит.

### Фаза 3 — «Полир»: качество интерфейса (Gemini-волна, 1–2 волны)
Дизайн — через Gemini по протоколу команды; факты движка и конвенции — от GLM.

- [ ] **Закрыть известные дыры:** Google AI Overview (нужна живая DOM-сессия с
      владельцем — runtime-классы), about:reader, PDF-viewer (pdf.js UI), print preview.
- [ ] **Чеклист-рейд всех about:-страниц:** preferences, addons, logins, downloads,
      protections, profiling — скриншот каждой, разбор Gemini, правки.
- [ ] **Меню B:** анимации открытия/закрытия (compositor-only), фокус-ринги,
      клавиатурная навигация, состояние hover/active у каждой кнопки.
- [ ] **Палитра:** задержка открытия ≤ 50 ms, пустое состояние поиска, счётчик
      команд.
- [ ] **Микро-типографика:** единая шкала размеров/letter-spacing (Gemini-аудит).
- [ ] Прогон: Пульс + бенчмарк + визуальный проход владельца. Коммит.

### Фаза 4 — «Сок»: фичи (1 волна, всё одобренного меню)
Всё прошло вето-фильтр конвенции 12 (польза каждый день ИЛИ вау без нагрузки).

- [x] **Звуковая волна 2.0 (BladeSounds 1.4.0, гит abad050):** шинг тембра новой
      темы при переключении (шина theme:changed, setTheme эмитит после щита);
      погодный тембр (дождь/снег → мягче-тише), ночной шёпот (blade.night ×0.6),
      приватный характер (окно приватки глуше; PrivateBrowsingUtils).
- [x] **Солнечные часы (BladeClock 2.7.0):** восход/закат из open-meteo
      (проверено живьём: Днепр 06:13/18:56, преф blade.clock.sun с кэшем на день),
      ночь по реальному солнцу, фолбэк 22:00-06:00 при отсутствии данных.
- [x] **Помодоро в палитре (BladePalette 1.2.0):** «Таймер: 25 минут (помодоро)»
      и «5 минут (перерыв)», фанфара тембра темы + вспышка окна на финише, живой
      остаток в метке «остановить». ПРОВЕРИТЬ ВЛАДЕЛЬЦУ: Ctrl+K руками —
      синтетический a11y-аккорд не открывает палитру (квирк ввода, не продукта).
- [x] **Battery-режим (BladeBattery 1.0.0):** navigator.getBattery жив в хроме
      (SAV_OFF на десктопе с зарядкой), атрибут+преф, CSS гасит бесконечные
      анимации (hero-дыхание/дрифты, живые фоны newtab через -moz-pref),
      idle-заставка не поднимается. Порог: не заряжается и <60%.
- [ ] Раннер на ошибке — не делан (не был одобрен явно).

### Фаза 5 — «Дом»: сетап и дистрибуция (1 волна)
- [x] **Full-пакет собран (2026-09-13 04:29, -SkipPublish):** Blade-Full-v2.0.0.zip
      149.2 МБ — движок владельца (FF155 + v3-хирургия) + chrome 2.0.0 побайтово
      из патча. Guard личных файлов пройден. Закрывает ограничение №5 (друзья
      получают свежий движок).
- [x] **Сетап пересобран:** BladeSetup\Output\Blade-Setup.exe (dotnet publish,
      свежий 04:29) + data.zip = Full 2.0.0 (VERSION внутри = 2.0.0, что и покажет
      установщик). Косметика: FileVersion экзешника в csproj остался 1.4.1.0 —
      не функционально (источник истины — VERSION из data.zip), поднять при
      желании в следующей сборке.
- [ ] **Артефакты у владельца на утро:** патч 2.0.0 (6.7 МБ) + Full (149.2 МБ) +
      Setup (exe+data.zip) — всё в Patches\ и BladeSetup\Output\. Живую установку
      НЕ делали (правило ночи). Публикация — по слову «публикуй».

### Фаза 3 — подготовка (ночь 2026-09-13)
- [x] **Gemini-промпт полира написан:** `Patches\gemini-prompt-polish.md` —
      reader/PDF/print/меню B/палитра с фактами движка и конвенциями.
      Запуск — после благословения владельца (протокол команды).

### Фаза 6 — «Выпуск» (1 волна)
- [ ] Финальный бенчмарк: таблица «до/после» в релиз-нотсы.
- [ ] FastHelper-финал: версии/счётчики/карта — ноль расхождений.
- [ ] Пульс на dev-машине и в песочнице — зелёный.
- [ ] Визуальный проход владельца по чеклисту фаз 2–4.
- [ ] Публикация по слову «публикуй».

## 3. Гварды (нарушить нельзя)

1. Контракт `window.Blade` (@loadOrder 5) — потребители Settings/ChromeStyle/Covers.
2. Конвенции 1–12 (BOM, mark-файлы, value-only скраб, без backdrop-filter на
   постоянных поверхностях, вето-фильтр фич).
3. Побайтовая идентичность chrome в Patch и Full (конвейер гарантирует — не ломать).
4. Профиль друзей не трогаем: сетап обновляет только App\Blade + chrome профиля.
5. Каждая волна = коммит + Пульс + бенчмарк; откат — через Backups\ по протоколу.
6. Публикация — только по слову владельца.

## 4. Открытые вопросы к владельцу (решить до старта фазы 1)

1. **1.9.6 опубликовать сейчас, до начала 2.0?** Рекомендую: да — друзья получат
   фикс часов и саундскрины уже сегодня, а 2.0 будем печь без спешки.
2. Кодовое имя релиза: предлагаю **«Переплавка»** (Full Reforge). Вето?
3. Раннер на странице ошибки — входит в фазу 4 или вето?

## 5. Фаза 7 — «Скелет»: полная чистка встроенного (инвентаризация 2026-09-13)

Цель владельца: «чтобы был только наш скелет, буквально всё наше» — ноль
телефонов Mozilla, ноль брендов Firefox в живых поверхностях. Инвентаризация
сделана по исходникам обоих omni.ja (все эндпоинты найдены грепом).

### A. Уже мертво (проверено по живым prefs + user.js)
- Ядро телеметрии: toolkit.telemetry.* (8 префов), datareporting.* (2)
- Обновления Mozilla: app.update.enabled=false + бинарники updater/pingsender/
  crashreporter стрипнуты конвейером Full
- Captive portal + connectivity-service, geo MLS (geo.provider.network.url="")
- DNS — свой механизм меню B (network.trr)
- Pocket/топстори/спонсорка newtab — killed ранее
- Coverage: toolkit.coverage.enabled=false по умолчанию движка

### B. Волна-B: консолидация киллов в user.js (СДЕЛАНО 2026-09-13)
Найден пробел: часть киллов жила только в prefs.js владельца — друзья их не
получали. Добавлено в user.js (имена сверены с greprefs.js/firefox.js):
app.normandy.enabled, app.shield.optoutstudies.enabled,
browser.discovery.enabled, browser.ping-centre.telemetry,
dom.push.connection.enabled (трейд-офф: фоновые push-уведомления сайтов
умирают — для личного браузера это фича, владелец предупреждён).

### C. Хирургия эндпоинтов (v3-конвейер, value-only + URL-гард) — НА УТВЕРЖДЕНИЕ
Техника: URL эндпоинтов в omni заменяются на about:blank — телефон невозможен
ФИЗИЧЕСКИ, даже если какой-то преф кто-то включит. Риск низкий (value-only,
наш же паттерн), но каждый прогон = перепрошивка движка друзьям (Full-пакет).
Кандидаты: telemetry.mozilla.org + incoming.telemetry.mozilla.org (9 файлов),
aus5.mozilla.org (апдейтер Mozilla — двойной пояс к app.update.enabled=false),
push.services.mozilla.com, accounts.firefox.com + monitor.firefox.com
(активны только при логине — мы не логинимся; скраб = гарантия),
contile.services.mozilla.com (спонсорные тайлы).

### D. ЗАЩИЩЕНО — НЕ РЕЖЕМ (безопасность друзей)
- firefox.settings.services.mozilla.com (Remote Settings): блоклисты OneCRL
  (отзыв сертификатов), блоклист дополнений, пины. Выключить = друзья без
  обновлений отозванных сертов. Держим.
- SafeBrowsing (Google-хосты): антифишинг/малварь. Держим (решение владельца).
- webcompat-инъекции: совместимость сайтов, из v3-хирургии ИСКЛЮЧЕНЫ осознанно.
- Сертификатная логика/OCSP/CRLite — не трогаем вообще.

### E. Бренд-остатки (полир после сессии с владельцем)
about:support, about:license (юридическая MPL — честно остаётся), заголовки
диалогов — прогон рейдом по about:-страницам (фаза 3 «Полир», промпт готов).

## 6. Риски

- **BobliksSettings-декомпозиция** — самый опасный шаг (меню B — самое юзаемое).
  Митигация: секции по одной, после каждой — Пульс + живой клик-тест владельца.
- **Сабсет шрифтов** — потеря глифа = кривой заголовок. Митигация: чек-список
  глифов (кириллица, латиница, цифры, пунктуация, «·», «—»), скрин-сверка до/после.
- **Setup-миграция** — чужие машины разнообразны. Митигация: песочница + лог
  установки (InstallerLogic уже пишет стадии), ручной прогон у одного друга-добровольца.
- **Сроки:** фазы 0–2 — двигают внутренности, владелец внешне почти ничего не
  увидит до фазы 3. Это нормально: сок выжимается незаметно, потом взрыв качества.

## 8. Фаза 8 — «Чистый Лист»: языки + AI-контролы (ПЛАН, 2026-09-13, ждёт слова владельца)

Разведка: Analyst (языки) + FastHelper (AI), оба отчёта верифицированы по исходникам.

### 8.1 Языки: каталог выбора (287 → 3)
- Источник длинного списка в настройках: `res/language.properties` в КОРНЕВОМ omni.ja
  (287 записей `код.accept=true`). Единственный потребитель — диалог добавления
  веб-языков (preferences/dialogs/languages.js/.xhtml). Проверено: правая колонка
  (активные языки) строится из intl.accept_languages (уже «ru»), ленгпак-диалог
  независим.
- РЕЗ: оставить `en.accept`, `en-us.accept`, `ru.accept` — 284 строки наружу.
- `languageNames.ftl` НЕ трогать: обслуживает спелчекер/нарратор/langpack-matcher,
  деградация не нужна (записи en/ru там есть).
- После правки omni — чистка startupCache (иначе stringbundle из кэша).
- **Переводчик выживет гарантированно**: модели качаются из Remote Settings
  (защищённый нами хост, проверено в TranslationsParent.sys.mjs), translations
  не читает ни browser.ml.enable, ни language.properties.

### 8.2 AI-контролы: нативный слой блокировки
В движке ЕСТЬ штатный слой: `browser.ai.control.<фича>` = default|available|
enabled|blocked; «blocked» = UI исчезает + скачанные модели удаляются.
⚠️ ГЛАВНАЯ МИНА: `browser.ai.control.default = "blocked"` рубит ВСЁ ВКЛЮЧАЯ
ПЕРЕВОДЧИКА. Только пофичево, translations не трогать.

Срез префами (user.js, без правки omni, UI исчезает сам):
- browser.ai.control.sidebarChatbot = "blocked" (+ ml.chat.* гаснут производно)
- browser.ai.control.smartWindow = "blocked" + browser.smartwindow.agent.enabled
  = false, memories.generateFromHistory/Conversation = false,
  autoTabGrouping.enabled = false, sidebar.openByDefault = false
- browser.ai.control.linkPreviewKeyPoints = "blocked" + ml.linkPreview.enabled = false
- browser.preferences.aiControls = false (вся категория AI из настроек — одной строкой)
- НЕ ТРОГАТЬ: browser.ai.control.translations (default)

Грей-зона — РЕШЕНИЕ ВЛАДЕЛЬЦА:
- smartTabGroups (ML-группировка вкладок) — блокировать или оставить?
- pdfjsAltText (AI-подписи картинок в PDF) — блокировать?
- browser.ml.enable = false (вся on-device ML-инфраструктура: убьёт semantic
  history поиск, urlbar ML, PDF alt-text; перевод НЕ тронет — проверено)
- extensions.ml.enabled = false (ML API для расширений)

Глубокий срез (omni, «буквально всё наше», опционально, с риск-метками):
- browser.xhtml: пункты меню AI-окна/контекстов (appMenu-new-ai-window, panelview
  ai-window-toggle-view, context-openlinksmartwindow, smartwindow-кнопки, сплиттер)
- ~60 файлов aiwindow/** (~700 КБ) + genai/** + UrlbarProviderAiChat
- ftl-строки: genai.ftl, aiWindow*.ftl, preferences.ftl:2222-2288, промо asrouter
- темы aiwindow/aiwindow-nova
- ⚠️ Удалять МОДУЛИ рискованно (кросс-импорты → исключения); безопасный путь —
  префы off + вырез DOM-узлов/ftl, модули оставить мёртвым грузом или резать
  по одному с Пульсом на каждом шаге

### 8.3 Бонус: новые скелет-эндпоинты (в срез-лист Apply-Blade-Skeleton)
Найдены при разведке AI: mlpa-prod-prod-mozilla.freetls.fastly.net (smartwindow),
merino.services.mozilla.com (worldcup/searchQuery; НЮАНС: merino также обслуживает
Firefox Suggest — у нас выключен), model-hub.mozilla.org (ML-модели). С blocked-
префами инертны, но физический скраб = гарантия. Добавить при следующем прогоне.

## 9. ПЛАН ВПЕРЁД + СТОП-ТОЧКА (владелец сонный, 2026-09-13 ~05:40)

> ⛔ СТОП: редактирование браузера остановлено по слову владельца. Следующие
> шаги — ТОЛЬКО по его новому слову. Ночная смена удалена, дозорных нет.

### 9.1 Ждёт одного слова «публикуй»
- 2.0.0 «Переплавка»: Blade-Patch-2.0.0.zip (6.7 МБ) + Blade-Full-v2.0.0.zip
  (149 МБ) + BladeSetup\Output (exe + data.zip). Всё пересобрано со скелетом-70
  и Чистым Листом v2. Перед публикацией — визуальный рейд владельца (9.2).

### 9.2 Визуальный рейд владельца (чек-лист, 5 минут)
1. Настройки → Язык → «Выберите язык…» — только English/Russian
2. Настройки → Язык интерфейса («Set Alternatives») — пусто, только English (US)
3. Настройки — секции AI не существует
4. Сайдбар — нет «AI chatbot»; правый клик по ссылке — нет «Ask chatbot»
5. Переводчик: страница на иностранном → иконка перевода работает
6. Ctrl+K → «Таймер: 25 минут»; темы в меню B — шинг голосом темы
7. Главная: большие часы + погода; вечером — ночь по солнцу Днепра (18:56)

### 9.3 Ближайшие шаги (по пробуждению)
- Мастер-пароль в about:logins (броня паролей, 1 минута)
- Установка сетапа: тест «1.9.6 → Setup 2.0» на живой машине/песочнице
- После публикации: подчистить непубликовавшиеся зипы в Patches\ (1.9.2–1.9.6)

### 9.4 Следующие волны (по приоритету владельца, ничего не начато)
1. Фаза 3 «Полир» — дизайн-волна через Gemini, промпт ГОТОВ:
   Patches\gemini-prompt-polish.md (reader/PDF/печать/меню B/палитра/типографика)
2. Google AI Overview затемнение — нужна живая DOM-сессия с владельцем
3. Декомпозиция BobliksSettings — карта Analyst готова, резать только при
   живых клик-тестах владельца (10 точек риска)
4. Диета памяти: dom.ipc.processCount.webIsolated 4→2, кэши медиа/картинок
   (StaticPrefs), замеры до/после на живых вкладках. Цель −10-20% RAM
5. Вырезание мёртвых AI-файлов (~700 КБ, aiwindow/** + genai/**) — по одному
   с Пульсом; moz-src НЕ трогать (реестр акторов на старте)
6. Косметика: FileVersion сетапа 1.4.1.0→2.0.0 в csproj; ротация Backups\

### 9.5 Заморожено / вето (не поднимать)
- Приватный след (неудобно — владелец), ML-группировка вкладок (заблокирована),
  source-форк Gecko (отвергнут навсегда — безопасность), вертикальные вкладки,
  тем-по-дням/хоткей-лабиринты (конвенция 12)
