# ROADMAP 2.0 — «Переплавка»

> План суперглобального релиза Blade 2.0: полный рефакторинг, выжимание скорости,
> высшее качество интерфейса, собственный сетап. Создан 2026-09-13 по слову владельца.
> Статус: **на утверждении**. Публикация только 2.0.0 в конце; промежуточные волны —
> сборки + визуальный проход владельца.

## 0. Цели и метрики успеха

| Метрика | Сейчас (замер 2026-09-12) | Цель 2.0 |
|---|---|---|
| Paint окна старта | 389 ms | **≤ 300 ms** |
| SSR (первый стабильный кадр) | 411 ms | **≤ 330 ms** |
| DCL / Load | 194 / 267 ms | ≤ 170 / ≤ 230 ms |
| Строки JS (11 скриптов) | 4543 | −15% без потери фич (дедуп) |
| Строки CSS (без covers) | 3614 | −15% (мёртвые правила волн 1.7–1.9) |
| Шрифты | 1.68 MB (Unbounded 778 KB!) | ≤ 0.7 MB (сабсет cyr+lat) |
| Известные визуальные дыры | 4 (AI Overview, reader, PDF, print) | **0** |
| Дистрибуция | только патчи | **Patch + Full + Setup.exe в одном релизе** |
| Друзья: движок | старый, ждут Full (ограничение №5) | свежий FF155 + v3-хирургия |

Замер — `BladePerf` → `perf_mark.txt`. Каждая волна 2.0 пишет до/после в
`perf_history.txt` (новый формат, фаза 0). Метрика = медиана 5 запусков.

## 1. Аудит текущего состояния (факты, не ощущения)

- **JS 4543 строк / 11 скриптов.** Монолит: `BobliksSettings` 1599 строк = 35% всего
  кода (меню B, темы, фоны, сплеш, ghost, DNS, бэкап — всё в одном).
- **CSS 3614 строк руками + 549 генерится** (covers). Волны 1.7–1.9 писали блоки
  поверх блоков — дубли селекторов и мёртвые правила почти гарантированы.
- **Шрифты 1.68 MB.** Unbounded 778 KB — вариативный, полный; реально нужны
  cyrillic + latin + базовая пунктуация.
- **Слушатели:** window-scoped умирают с окном (утечек нет), но интервалы/страховки
  размазаны по скриптам без реестра; mountClock до сих пор тикает страховками
  setTimeout 1500/5000 из часового дела.
- **Старт:** сплеш и темы уже этапные; paint 389 ms — в бюджет, но CSS-диета и
  отложенные операции дадут ещё.
- **Сетап:** Blade-Setup.exe (.NET 9, single-file, 65 MB) жив и читает VERSION из
  data.zip — но его data.zip от 11 сентября (эпоха ~1.4.x, на восемь волн старше).
- **Конвейер Full** (`Publish -Mode Full`) уже даёт data.zip побайтово идентичный
  патчу — фундамент для 2.0-сетапа готов, надо собрать свежий и прогнать.
- **Движок:** FF155 с v3-хирургией у владельца; друзья на старом (ограничение №5).

## 2. Фазы (порядок зафиксирован: сначала инструменты, потом мясо)

### Фаза 0 — «Стенд»: бенчмарки и Пульс (инструменты, 1 волна)
Без замеров оптимизация — гадание. Сначала измерительный контур.

- [x] **BladePerf 2.0:** история замеров `perf_history.txt` (ротация 50),
      фазы dcl/load/paint/ssr. Стартовый прогон = базовая линия «до».
- [x] **Пульс Клинка** (`BladePulse.uc.js`): контракт `window.Blade`, узлы
      (#blade-hero-wrap, часы, меню B, навбар), шрифты, VERSION/covers →
      `blade_health.txt` + вердикт GREEN/YELLOW/RED. Первый живой прогон:
      GREEN, все 10 проверок ОК (2026-09-13 03:53).
- [x] **FastHelper-аудит базовой линии:** см. фазу 2 (находки уже там).
- [x] **Бейзлайн 2.0 (медиана 4 прогонов, 2026-09-13 03:54):**
      dcl=215ms load=285ms paint=410ms ssr=433ms.
- [x] Уроки ночи → конвенция 13 (PS 5.1 quoting, лаунчер-PID, dev-инстанс).

### Фаза 1 — «Ядро»: JS-рефакторинг (2–3 волны)
Цель: тот же функционал, меньше кода, ноль тиков вхолостую.

- [x] **1-А (гит 5eb0e64):** реестр `Blade.every/Blade.listen` с авто-снятием на
      unload; мигрированы интервалы Clock (погода) / Newtab (тик) / Settings
      (idle, авто-тема, idle-часы); срезан слепой 5с-ретрай mountClock.
      Верификация: Пульс GREEN, перф без регрессии (dcl 213/221 против 215).
- [ ] **1-Б: декомпозиция BobliksSettings** — карта Analyst готова (10 точек
      риска расписаны, порядок секций: Core→Themes→Bgs→System→Splash→Ghost→
      Menu→init→API→mark). САМЫЙ рискованный шаг — вживлять по одной секции,
      после каждой Пульс + живой клик-тест. Если ночь не потянет — отложить
      с обоснованием в отчёт (UI-ценность волны нулевая, риск высокий).
- [ ] Отложенный старт остальных тяжёлых операций (аудит после 1-Б).

### Фаза 2 — «Стиль»: CSS-диета (1–2 волны)
- [x] **Аудит мёртвого CSS (FastHelper, верифицировано GLM):** за 9 волн накопилось
      всего 2 мёртвых вещи — класс `.blade-newwin` (userChrome.css:109,126-128 —
      дубль #nav-bar::before, никто не вешает) и переменная `--bob-accent-soft`
      (пишется в userContent ~10 строк + Settings:213,269,279, не читается нигде).
      ВНИМАНИЕ: `--accent-soft` (хромовый) — ЖИВОЙ, это другая переменная.
      53/53 keyframes живые. CSS чище ожидаемого — фаза смещается к шрифтам/склейке.
- [x] **Сабсет шрифтов (гит 564f818):** 1.68 МБ → 551 КБ (−67%). Unbounded 778→209,
      Rubik 360→126, Rubik-Italic 355→124, JBM 187→106. Юникод: latin+cyrillic+
      пунктуация+₴/№/❯; вариативные оси wght целы, name-таблицы целы (Пульс
      GREEN — все три семьи регистрируются). Оригиналы: Backups\fonts-full-2026-09-13.
      JBM: ₴ в оригинале отсутствовал (системный фолбэк) — не потеря.
- [x] **Мёртвые артефакты срезаны:** блок .blade-newwin (дубль правила) +
      18 деклараций --bob-accent-soft + 3 сайта записи в Settings.
- [ ] **Склейка дублей** волн 1.7–1.9 — остаётся (крон-смена, по приоритету ниже 1-Б).
- [ ] **Мёртвые правила:** удалить классы без потребителей (список из фазы 0,
      ручная верификация каждого — правки только после grep-подтверждения).
- [ ] **Склейка дубликатов** волн 1.7–1.9: 13 микро-вау несли свои блоки поверх
      старых — объединить селекторы, свернуть повторы.
- [ ] **userContent:** сгруппировать по @-moz-document, убрать копипасту доменных
      блоков (Google-слои, погода-оверлеи).
- [ ] **Дорогие селекторы:** universal/глубокые descendant в горячих путях (табы,
      урлбар) → заменить классами-хуками, где движок позволяет.
- [ ] **Сабсет шрифтов:** Unbounded/Rubik/Rubik-Italic/JetBrains → cyr+lat+пунктуация.
      Проверить спецсимволы UI (✨ ☀ ✦ берутся системным fallback — убедиться, что
      сабсет их не сломал). Цель ≤ 0.7 MB суммарно.
- [ ] covers.css: минификация вывода генератора.
- [ ] Прогон: Gemini-скринрейд ключевых поверхностей + бенчмарк. Коммит.

### Фаза 3 — «Полир»: качество интерфейса (Gemini-волна, 1–2 волны)
Дизайн — через Gemini по протоколу команды; факты движка и конвенции — от GLM.

- [ ] **Закрыть известные дыры:** Google AI Overview (нужна живая DOM-сессия с
      владельцем — runtime-классы), about:reader, PDF-viewer (pdf.js UI), print preview.
- [ ] **Чеклист-рейд всех about:-страниц:** preferences, addons, logins, downloads,
      protections, profiling — скриншот каждой, разбор Gemini, правки.
- [ ] **Меню B:** анимации открытия/закрытия (compositor-only), фокус-ринги,
      клавиатурная навигация, состояние hover/active у каждой кнопки.
- [ ] **Палитра:** задержка открытия ≤ 50 ms, пустое состояние поиска, счётчик
      команд.
- [ ] **Микро-типографика:** единая шкала размеров/letter-spacing (Gemini-аудит).
- [ ] Прогон: Пульс + бенчмарк + визуальный проход владельца. Коммит.

### Фаза 4 — «Сок»: фичи (1 волна, всё одобренного меню)
Всё прошло вето-фильтр конвенции 12 (польза каждый день ИЛИ вау без нагрузки).

- [x] **Звуковая волна 2.0 (BladeSounds 1.4.0, гит abad050):** шинг тембра новой
      темы при переключении (шина theme:changed, setTheme эмитит после щита);
      погодный тембр (дождь/снег → мягче-тише), ночной шёпот (blade.night ×0.6),
      приватный характер (окно приватки глуше; PrivateBrowsingUtils).
- [x] **Солнечные часы (BladeClock 2.7.0):** восход/закат из open-meteo
      (проверено живьём: Днепр 06:13/18:56, преф blade.clock.sun с кэшем на день),
      ночь по реальному солнцу, фолбэк 22:00-06:00 при отсутствии данных.
- [x] **Помодоро в палитре (BladePalette 1.2.0):** «Таймер: 25 минут (помодоро)»
      и «5 минут (перерыв)», фанфара тембра темы + вспышка окна на финише, живой
      остаток в метке «остановить». ПРОВЕРИТЬ ВЛАДЕЛЬЦУ: Ctrl+K руками —
      синтетический a11y-аккорд не открывает палитру (квирк ввода, не продукта).
- [x] **Battery-режим (BladeBattery 1.0.0):** navigator.getBattery жив в хроме
      (SAV_OFF на десктопе с зарядкой), атрибут+преф, CSS гасит бесконечные
      анимации (hero-дыхание/дрифты, живые фоны newtab через -moz-pref),
      idle-заставка не поднимается. Порог: не заряжается и <60%.
- [ ] Раннер на ошибке — не делан (не был одобрен явно).

### Фаза 5 — «Дом»: сетап и дистрибуция (1 волна)
- [ ] Свежий **Full-пакет**: движок владельца (FF155 + v3-хирургия) + chrome 2.0 →
      data.zip (конвейер уже даёт побайтовую идентичность патчу).
- [ ] **Пересборка Blade-Setup.exe** с новым data.zip; версия читается из data.zip —
      провериться, что покажет 2.0.0.
- [ ] **Чистовая установка в песочнице** (fresh VM/новый юзер Windows): установка →
      первый запуск → сплеш → Пульс зелёный → дефолт-браузер одним кликом меню B.
- [ ] **Миграция друзей с патчей 1.9.x:** setup ставится поверх существующего
      %LOCALAPPDATA%\Blade, профиль Data\profile не трогается, VERSION перепрошивается.
      Протестировать сценарий «установлен 1.9.6 → запустил Setup 2.0».
- [ ] Это же закрывает **ограничение №5** (друзья получают новый движок).
- [ ] GitHub-релиз 2.0.0: Blade-Patch-2.0.0.zip + Blade-Full-2.0.0.zip +
      Blade-Setup.exe (+ data.zip). Апдейтер друзьям подскажет: патч — сам, Full —
      по желанию, Setup — для новых.

### Фаза 6 — «Выпуск» (1 волна)
- [ ] Финальный бенчмарк: таблица «до/после» в релиз-нотсы.
- [ ] FastHelper-финал: версии/счётчики/карта — ноль расхождений.
- [ ] Пульс на dev-машине и в песочнице — зелёный.
- [ ] Визуальный проход владельца по чеклисту фаз 2–4.
- [ ] Публикация по слову «публикуй».

## 3. Гварды (нарушить нельзя)

1. Контракт `window.Blade` (@loadOrder 5) — потребители Settings/ChromeStyle/Covers.
2. Конвенции 1–12 (BOM, mark-файлы, value-only скраб, без backdrop-filter на
   постоянных поверхностях, вето-фильтр фич).
3. Побайтовая идентичность chrome в Patch и Full (конвейер гарантирует — не ломать).
4. Профиль друзей не трогаем: сетап обновляет только App\Blade + chrome профиля.
5. Каждая волна = коммит + Пульс + бенчмарк; откат — через Backups\ по протоколу.
6. Публикация — только по слову владельца.

## 4. Открытые вопросы к владельцу (решить до старта фазы 1)

1. **1.9.6 опубликовать сейчас, до начала 2.0?** Рекомендую: да — друзья получат
   фикс часов и саундскрины уже сегодня, а 2.0 будем печь без спешки.
2. Кодовое имя релиза: предлагаю **«Переплавка»** (Full Reforge). Вето?
3. Раннер на странице ошибки — входит в фазу 4 или вето?

## 5. Риски

- **BobliksSettings-декомпозиция** — самый опасный шаг (меню B — самое юзаемое).
  Митигация: секции по одной, после каждой — Пульс + живой клик-тест владельца.
- **Сабсет шрифтов** — потеря глифа = кривой заголовок. Митигация: чек-список
  глифов (кириллица, латиница, цифры, пунктуация, «·», «—»), скрин-сверка до/после.
- **Setup-миграция** — чужие машины разнообразны. Митигация: песочница + лог
  установки (InstallerLogic уже пишет стадии), ручной прогон у одного друга-добровольца.
- **Сроки:** фазы 0–2 — двигают внутренности, владелец внешне почти ничего не
  увидит до фазы 3. Это нормально: сок выжимается незаметно, потом взрыв качества.
