# ЗАДАЧА ДЛЯ ГЕМИНИ: «Новый Силуэт» — вертикальные вкладки, плавающая капсула, плитки 3D, заставки

## КОНТЕКСТ

Проект Blade (Firefox 155, uc.js). ПЕРВЫМ ДЕЛОМ прочитай:
1. `F:\firefox michael edition\PROJECT_MAP.md` — карта проекта;
2. `F:\firefox michael edition\Patches\gemini-prompt-design.md` — конвенции и факты движка (действуют все);
3. `F:\firefox michael edition\Patches\gemini-prompt-living.md` — хуки погоды/ночи/музыки (действуют).

Инженерные хуки волны УЖЕ в коде (не трогать их логику). Правки точечные (Edit), UTF-8 без BOM, node --check на каждом тронутом .uc.js. Это волна СИЛУЭТА — самое смелое, но конвенции перфа неприкосновенны: в @keyframes только transform/opacity; backdrop-filter НЕЛЬЗЯ на больших постоянных поверхностях (навбар/сайдбар/оверлеи) — полупрозрачные фоны вместо стекла с блюром.

## ХУКИ ВОЛНЫ (уже работают)

- **Вертикальные вкладки**: нативный преф `sidebar.verticalTabs`; при включении BobliksSettings ставит атрибут `[data-blade-vtabs]` на `<html>` (живое переключение). Глухое скрытие сайдбара уже снимается под этим атрибутом (userChrome.css, блок с комментарием «Вертикальные вкладки»).
- **Заставка простоя**: div#blade-idle (создаёт BobliksSettings при 3+ мин бездействия, гварды: fullscreen/музыка/преф blade.idle.on): pointer-events:none, fixed inset 0, внутри .bi-word ('B L A D E') и .bi-clock (HH:MM). Стили — инъекция в JS, см. ниже.
- **Сплеш старта**: #blade-splash (img + .word 'B L A D E'), живёт 1.55с, «шинг» звучит на ~0.7с.

## ЗАДАЧА 1: ВЕРТИКАЛЬНЫЕ ВКЛАДКИ BLADE (userChrome.css)

Сначала УСТАНОВИ СТРУКТУРУ: распакуй `C:\Users\Deni\AppData\Local\Blade\App\Blade\browser\omni.ja` (обычный zip, во временную папку) и изучи `chrome/browser/content/browser/browser.xhtml` (grep `vertical-tabs` — 6 вхождений: там тулбар/скроллбокс вертикальной ленты) и при необходимости `chrome/browser/content/browser/sidebar/browser-sidebar.js`. Пиши селекторы ТОЛЬКО по реально найденным элементам.

Стилизация (всё под `html|html[data-blade-vtabs]`):
- колонка: фон `color-mix(in srgb, var(--bg) 82%, transparent)` (лёгкая полупрозрачность, БЕЗ блюра), правая граница 1px акцент-soft;
- вкладка: скругление 8px, паддинги, favicon нормального размера, заголовок `var(--blade-ui)` 12px, ellipsis;
- активная: акцентная подложка 16% + ромб-корона (клеймо проекта — посмотри, как сделан ромб на горизонтальной активной вкладке в userChrome, секция «корона», и повтори приём);
- hover: подложка 8%;
- `[soundplaying]`-вкладка — мягкая opacity-пульсация (музыкальный пульс проекта);
- ширина колонки ~240px, при наведении на границу — расширение НЕ делать (ломает перф), просто аккуратный скролл: scrollbar-width thin, акцентные цвета;
- НИЧЕГО не делать с сайдбаром, когда атрибута нет (дефолт — скрыт).

## ЗАДАЧА 2: ПЛАВАЮЩАЯ КАПСУЛА ТУЛБАРА (userChrome.css)

Навбар отрывается от краёв и парит (стиль Arc, но в духе Blade):
- `#navigator-toolbox` уже имеет margin 2px (секция «рамка окна» ~строки 985-1005 — прочитай, не сломай кайму окна!); капсула: `#nav-bar` — margin 6px 10px 4px, border-radius 14px, фон `color-mix(in srgb, var(--panel) 88%, transparent)`, граница 1px акцент-soft, тень вниз 12px 34px rgba(0,0,0,0.45) СТАТИКОЙ;
- при фокусе урлбара капсула получает акцентную рамку (есть существующий фокус-стиль — усиль связку, но не задень .urlbar::after «обнажение»);
- ЗАПРЕЩЕНО менять высоту навбара и позиционирование таббара — только фон/рамки/отступы/тени; тулбокс без #nav-bar (таббар-полоса) должен остаться визуально согласованным (тонкая подложка, не капсула);
- в fullscreen и maximized — существующие исключения рамки окна сохраняются.

## ЗАДАЧА 3: ПЛИТКИ-КАРТОЧКИ 3D (userContent.css, about:home/newtab)

- `.top-site-outer`: perspective на родителе (.topsites-section или общем контейнере — проверь реальную структуру в userContent/covers), при :hover плитка наклоняется `transform: perspective(600px) rotateX(2.5deg) rotateY(-3deg) translateY(-3px)` (только transform, transition 0.18s);
- иконка внутри — лёгкий scale(1.04);
- свечение по контуру — статикой (box-shadow при hover);
- ВНИМАНИЕ: `.tile::after` уже занят СЛЭШЕМ (волна «Живой Клинок») — не трогать его, 3D живёт на самом .tile/.top-site-icon;
- не ломать: монограммы-фолбэки covers.css, existing hover-стили сведи к одному источнику правды (если конфликт — 3D побеждает, старый hover-фон убери).

## ЗАДАЧА 4: SPLASH 2.0 + ЗАСТАВКА ПРОСТОЯ (CSS-константы в BobliksSettings.uc.js)

Оба стиля — инъекционные CSS-СТРОКИ внутри `JS\BobliksSettings.uc.js` (блок сплеша ~1190-1240, блок idle рядом — найди по '#blade-idle'). Правь ТОЛЬКО CSS-строки, логику/тайминги JS не трогать. После правок — `node --check`.

- **Splash 2.0**: .word — `var(--blade-display)`, 34px, letter-spacing 16px, акцент с свечением; под словом — лёгкий блик-линия (::after на #blade-splash), которая проезжает transform'ом за ~0.9s (к моменту «шинга» на 0.7с — свет и звук встречаются); img (клинок) — масштаб 120px, лёгкий drop-shadow. Общий вход — opacity/transform, существующие keyframes можно заменить на свои (transform/opacity only).
- **Заставка простоя** (#blade-idle): глубокий тёмный фон #040406 с медленным радиальным акцентным дыханием (градиент статичен, дышит ОРАСИТИ элемента через opacity-анимацию 6-9s); .bi-word — Unbounded-стек, 26px, letter-spacing 14px, opacity 0.85, едва заметное «дыхание»; .bi-clock — var(--blade-mono), 96px, вес 200, светло-серый с лёгким акцентным свечением; внизу мелким mono — «Blade ждёт твоего возвращения» (добавь как ::after контейнер? НЕТ — markup менять нельзя; вместо этого стилизуй .bi-clock::after с content и переводом строки не мудри — просто оставь два элемента, а подпись НЕ добавляй, если нельзя без JS). Всплеск появления — 0.6s fade+scale (transform/opacity).

## ЧЕГО НЕ ДЕЛАТЬ

- Не трогать: логику JS (кроме CSS-строк), хуки [data-blade-vtabs]/#blade-idle, обнажение клинка, живые темы 5.1, шрифтовые блоки, слэш, осадки, панель палитры.
- Не добавлять backdrop-filter на навбар/сайдбар/idle-оверлей.
- Не менять высоты/раскладку тулбара и таббара.

## ПРОВЕРКА (сделай сам до сдачи)

1. `node --check` на BobliksSettings.uc.js; скобки в userChrome/userContent.
2. grep: нет backdrop-filter у #nav-bar/#sidebar/#blade-idle; нет keyframes-свойств кроме transform/opacity в НОВЫХ анимациях; `.tile::after` не переопределён; селекторы вертикальных вкладок реально существуют в browser.xhtml (перечисли их в отчёте с номерами строк из движка!).
3. Отчёт: по каждой задаче — селекторы, что проверил, что требует визуальной проверки человеком (в т.ч. переключение вкладок вертикально/горизонтально на живом окне).
